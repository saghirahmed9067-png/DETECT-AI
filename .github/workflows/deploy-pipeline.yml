# This workflow is an alias for deploy-cf-pipeline.yml
# The canonical deploy is in deploy-cf-pipeline.yml (5 workers: a/b/c/d/e)
name: Deploy Pipeline (alias)

on:
  workflow_dispatch:

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "Use 'Deploy Cloudflare Pipeline (5 Workers)' workflow instead."
          echo "That workflow deploys: detectai-pipeline, -b, -c, -d, -e"
