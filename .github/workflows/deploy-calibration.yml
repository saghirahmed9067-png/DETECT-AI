# Calibration is now handled by GitHub Actions (.github/workflows/calibrate.yml)
# This workflow is kept only for historical reference - DO NOT RUN
# The CF calibration workers have been replaced by the GH Actions approach
# which processes 500 real images with no CPU limits.
name: "[DEPRECATED] CF Calibration Workers"
on:
  workflow_dispatch:
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Calibration now runs via GitHub Actions (calibrate.yml)"
          echo "See .github/workflows/calibrate.yml for the active workflow"
          exit 1
