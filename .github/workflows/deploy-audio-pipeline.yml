# Audio pipeline workers were removed (had PLACEHOLDER_AUDIO_D1_ID, never deployed).
# Audio sources are now handled by the main 5 workers via cf-pipeline/src/sources/audio.ts
# This workflow is kept for reference only — DO NOT RUN.
name: "[DISABLED] Deploy Audio Pipeline"

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Audio pipeline is now handled by the main 5 CF workers."
          echo "Audio sources: cf-pipeline/src/sources/audio.ts (15 sources)"
          echo "Run 'Deploy Cloudflare Pipeline (5 Workers)' instead."
          exit 1
