# Admin auth
ADMIN_PASSWORD=           # bcrypt hash (starts with $2b$) for production; plaintext for dev
ADMIN_SESSION_SECRET=     # random 32-char secret for HMAC session signing

# Supabase (service role ONLY — never anon key)
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Cloudflare (read-only token for pipeline stats)
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_D1_DATABASE_ID=
